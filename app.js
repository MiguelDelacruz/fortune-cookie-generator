var fortuneList = ["You will get a walnut",
"The time has come",
"pokemons are real",
"I will get extra credit for trying",
"theres a snake in my boot",
"reach for the sky"]
document.getElementById('fortune-cookie-text')

function generateFortruneCookie () {
	if  (randomFortune == 0) {
	return output = fortuneList[0];

	else if  (randomFortune == 1) {
	return output = fortuneList[1]
}
}
